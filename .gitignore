# Local snapshots taken before large edits - git history replaces these
*.bak
*.pre18
*.predark

# Never commit personal setup data
budget-setup-PRIVATE.json

# OS noise
Thumbs.db
.DS_Store

# React/Vite build tooling - docs/ (the built site GitHub Pages serves)
# IS committed, only the tooling and its cache are not.
node_modules
.vite
